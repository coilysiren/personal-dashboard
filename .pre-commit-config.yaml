repos:
    
  
  
  
  # BEGIN managed by agentic-os/scripts/apply-agentic-os-hooks.py
  - repo: https://github.com/coilysiren/agentic-os
    rev: v0.2.2
    hooks:
      # catalog-doc-size + catalog-trifecta deferred: AGENTS.md is a symlink to canonical agentic-os-kai/AGENTS.md.
      # The size cap will re-enable with the canonical AGENTS skill-split (coilysiren/agentic-os-kai#557);
      # trifecta needs symlink-transparent path resolution in the validator before it can run here.
      # - id: catalog-doc-size
      # - id: catalog-trifecta
      - id: catalog-block-present
      - id: validate-skills
      - id: dead-cross-links
      - id: closes-issue
      - id: conventional-commit
  # END managed by agentic-os/scripts/apply-agentic-os-hooks.py
